<!-- <% if logged_in? %>
  <p class="pull-right">
    [<%= link_to "Edit this category", edit_category_path(@category) %>]
  </p>
<% end %> -->

<% all_posts_link = link_to("All Posts", posts_path) %>

<%= render "shared/content_title",
           title: "#{all_posts_link} &raquo; #{@category.name}".html_safe %>

<%= render @posts %>
